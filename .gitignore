# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# Data files (keep models for deployment)
*.csv
*.pkl
# Note: models/*.joblib and models/*.json are needed for deployment, so we commit them

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Streamlit (but keep config.toml)
.streamlit/secrets.toml
.streamlit/credentials.toml

# Training artifacts (not needed for deployment)
catboost_info/
*.tsv
*.log
weight_optimization_results.csv
feedback.txt
app.log

# Documentation build (if any)
docs/_build/
docs/.doctrees/

# Scripts (keep them in repo, but ignore compiled files)
scripts/__pycache__/
scripts/*.pyc

# Docker (optional - can include if needed)
# docker-compose.train.yml
# Dockerfile.train

